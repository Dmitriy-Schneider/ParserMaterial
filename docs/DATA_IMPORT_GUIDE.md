# Руководство по добавлению новых марок в базу данных

## 📊 Сводка результатов

**Текущее состояние базы данных:**
- Всего марок: **10,394**
- Источник: splav-kharkov.com (полное покрытие - 3,299 марок проверено)
- Чугуны: 122 марки (включая ЧНХМДШ ✓)
- Дубликатов: 0

**Статус парсера splav-kharkov.com:**
- ✅ База ПОЛНАЯ относительно сайта
- ✅ Все 27 типов материалов обработаны
- ✅ Telegram bot обновлен
- ✅ Docker контейнеры актуальны

---

## 🎯 Лучшие форматы для добавления новых марок

### Рекомендация #1: **Веб-сайт марочника** 🌐
**Приоритет: ВЫСОКИЙ** ⭐⭐⭐

**Почему это лучший вариант:**
- ✅ Автоматизированный парсинг (экономит время)
- ✅ Структурированные данные (химсостав, аналоги, стандарты)
- ✅ Актуальная информация
- ✅ Можно обновлять периодически
- ✅ Минимум ручной работы

**Требования к сайту:**
- HTML структура с таблицами или списками
- Химический состав в удобочитаемом формате
- Желательно: ссылки на аналоги, стандарты (ГОСТ, DIN, ASTM)

**Пример успешного парсинга:**
```bash
python parsers/splav_kharkov_advanced.py
# Результат: 3,299 марок проверено, 0 ошибок
```

**Что нужно от вас:**
1. URL сайта марочника
2. Пример ссылки на конкретную марку
3. Скриншот структуры страницы (опционально)

**Я создам парсер под этот сайт!**

---

### Рекомендация #2: **Excel таблица** 📊
**Приоритет: СРЕДНИЙ** ⭐⭐

**Почему это хороший вариант:**
- ✅ Структурированные данные
- ✅ Легко парсить (pandas)
- ✅ Можно импортировать массово
- ⚠️ Требует ручной подготовки данных

**Идеальный формат Excel:**

| Марка | Стандарт | C | Cr | Ni | Mo | V | W | Co | Mn | Si | Cu | Nb | N | S | P | Аналоги | Производитель |
|-------|----------|---|----|----|----|----|---|-----|----|----|----|----|---|---|---|---------|---------------|
| ЧНХМДШ | GOST | 3-3.6 | 0.2-0.4 | 0.6-1 | 0.2-0.6 | | | | | | | | | | | ЧН15Д3Ш, ЧН19Х3Ш | |
| AISI 304 | ASTM | 0.08 | 18-20 | 8-10.5 | | | | | 2 | 1 | | | | 0.03 | 0.045 | 1.4301 08Х18Н10 | |

**Требования:**
1. Первая строка = заголовки колонок
2. Обязательные колонки: **Марка**, **C** (хотя бы один элемент)
3. Разделители диапазонов: тире "-" (например "0.17 - 0.24")
4. Аналоги: через пробел или запятую
5. Кодировка: UTF-8

**Скрипт для импорта Excel:**
```python
python scripts/import_from_excel.py --file "путь/к/файлу.xlsx"
```

*(Я создам этот скрипт, если дашь Excel файл!)*

---

### Рекомендация #3: **PDF файл** 📄
**Приоритет: НИЗКИЙ** ⭐

**Почему это сложнее:**
- ⚠️ Неструктурированные данные
- ⚠️ Сложно парсить (OCR, tabula-py)
- ⚠️ Возможны ошибки распознавания
- ⚠️ Требует много ручной проверки

**Когда использовать PDF:**
- Если данные ТОЛЬКО в PDF
- Если таблицы четкие и структурированные
- Если нет альтернативы

**Требования к PDF:**
1. Таблицы, не сканы (текстовый слой)
2. Четкая структура (колонки выровнены)
3. Один тип данных на страницу

**Скрипт для PDF:**
```python
python scripts/import_from_pdf.py --file "марки.pdf"
```

*(Потребует доработки под конкретный формат)*

---

## 🔧 Создание нового парсера для сайта

Если у вас есть **веб-сайт марочника**, я создам специализированный парсер по этому шаблону:

### Шаг 1: Предоставьте информацию
```
1. URL главной страницы:
   Пример: https://example.com/marochnik

2. URL примера марки:
   Пример: https://example.com/marochnik/grade/304

3. Особенности:
   - Есть ли классификация по типам?
   - Как организованы ссылки на марки?
   - Есть ли аналоги на странице?
```

### Шаг 2: Я создаю парсер
```python
# parsers/your_site_parser.py
class YourSiteParser:
    def parse_all(self):
        # Автоматически:
        - Находит все марки
        - Парсит химсостав
        - Извлекает аналоги
        - Проверяет дубликаты
        - Добавляет в БД
```

### Шаг 3: Запуск
```bash
python parsers/your_site_parser.py
# Результат: N новых марок добавлено
```

---

## 📋 Формат для ручного добавления

Если марок немного (< 50), можно добавить вручную через SQL:

```sql
INSERT INTO steel_grades (
    grade, standard, c, cr, ni, mo, v, w, co, mn, si, cu, nb, n, s, p, analogues
) VALUES (
    'НОВАЯ_МАРКА',
    'GOST',
    '0.17 - 0.24',  -- C
    '0.25',         -- Cr
    NULL,           -- Ni
    NULL,           -- Mo
    NULL,           -- V
    NULL,           -- W
    NULL,           -- Co
    '0.35 - 0.65',  -- Mn
    '0.17 - 0.37',  -- Si
    '0.25',         -- Cu
    NULL,           -- Nb
    NULL,           -- N
    'до 0.04',      -- S
    'до 0.035',     -- P
    'AISI 1020 SAE 1020'  -- Analogues
);
```

**Или через Python:**
```python
from database_schema import get_connection

conn = get_connection()
cursor = conn.cursor()

cursor.execute("""
    INSERT INTO steel_grades (grade, standard, c, cr, ni)
    VALUES (?, ?, ?, ?, ?)
""", ('НОВАЯ_МАРКА', 'GOST', '0.17-0.24', '0.25', None))

conn.commit()
conn.close()
```

---

## ✅ Рекомендованный workflow

### Для массового добавления (> 100 марок):

1. **Веб-сайт** → Я создаю парсер → Автоматический импорт
2. **Excel** → Подготовка файла → Скрипт импорта
3. **PDF** → Конвертация в Excel → Скрипт импорта

### Для небольших партий (< 100 марок):

1. **Excel** → Прямой импорт
2. **Ручной ввод** через SQL/Python

---

## 📝 Пример Excel шаблона

Скачайте: [steel_grades_template.xlsx](templates/steel_grades_template.xlsx)

Или создайте с такими колонками:

```
Марка | Стандарт | C | Cr | Ni | Mo | V | W | Co | Mn | Si | Cu | Nb | N | S | P | Аналоги
```

**Правила заполнения:**
- Диапазоны: "0.17 - 0.24" или "0.17-0.24"
- Максимум: "до 0.04" или "< 0.04"
- Отсутствует: пустая ячейка
- Аналоги: через пробел "AISI 304 1.4301 08Х18Н10"

---

## 🚀 Что делать дальше?

### Вариант 1: У вас есть сайт марочника
→ Дайте мне URL, я создам парсер за 30 минут!

### Вариант 2: У вас есть Excel файл
→ Отправьте файл или создайте по шаблону выше
→ Я создам скрипт импорта

### Вариант 3: У вас есть PDF файл
→ Если PDF единственный вариант, отправьте
→ Попробую создать парсер (может потребовать корректировки)

### Вариант 4: Небольшой список марок
→ Можно добавить вручную через SQL
→ Или создайте mini-Excel на 10-20 марок

---

## 📊 Статистика текущей базы

```sql
-- Всего марок
SELECT COUNT(*) FROM steel_grades;
-- Результат: 10,394

-- Марки по типам (примерно)
SELECT
    CASE
        WHEN grade LIKE 'СЧ%' OR grade LIKE 'КЧ%' OR grade LIKE 'ВЧ%' THEN 'Cast Iron'
        WHEN grade LIKE '%Х%' AND (grade LIKE '%Н%' OR grade LIKE '%Cr%') THEN 'Stainless'
        WHEN grade LIKE 'Д%' OR grade LIKE 'АМ%' OR grade LIKE 'АК%' THEN 'Aluminum'
        WHEN grade LIKE 'Бр%' THEN 'Bronze'
        WHEN grade LIKE 'Л%' THEN 'Brass'
        ELSE 'Other Steel'
    END as type,
    COUNT(*) as count
FROM steel_grades
GROUP BY type;
```

---

## 🎯 Итоговая рекомендация

**Для максимальной эффективности:**

1. **Приоритет #1:** Дайте ссылки на сайты марочников → Автоматизированный парсинг
2. **Приоритет #2:** Excel таблицы → Быстрый импорт скриптом
3. **Приоритет #3:** PDF только если нет других вариантов

**Для качества данных:**
- Сайты > Excel > PDF > Ручной ввод

**Что мне нужно от вас:**
- 🌐 **Ссылки на сайты** (лучший вариант!)
- 📊 **Excel файлы** в формате выше
- 📄 **PDF файлы** как последний вариант

---

## 💡 Бонус: Проверка качества данных

После импорта обязательно:

```bash
# Проверка дубликатов
SELECT grade, COUNT(*) as count
FROM steel_grades
GROUP BY grade
HAVING count > 1;

# Проверка пустых марок
SELECT COUNT(*) FROM steel_grades WHERE grade IS NULL OR grade = '';

# Проверка химсостава (хотя бы один элемент)
SELECT COUNT(*) FROM steel_grades
WHERE c IS NULL AND cr IS NULL AND ni IS NULL AND mo IS NULL;
```

---

**Жду от вас данные в любом формате!**
Готов создать парсер или скрипт импорта под ваши нужды. 🚀
