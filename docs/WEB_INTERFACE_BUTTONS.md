# Web интерфейс - Кнопки поиска

## Дата: 2026-01-08

## Три типа поиска в Web интерфейсе

### 1. 📝 Обычный поиск (автоматический)

**Как работает:**
- Просто начните вводить название марки в поле "Grade"
- Автоматический поиск запускается через 1.5 секунды после ввода

**Тип поиска:**
- Частичное совпадение в БД (SQL: `LIKE '%название%'`)
- **БЕЗ AI**

**Примеры:**
```
Ввод: "K"     → Находит: K888, K340, K110, SKH51, и т.д.
Ввод: "K88"   → Находит: K888
Ввод: "Bohler" → Находит: все марки Bohler
```

**Расход токенов:** ❌ НЕТ

---

### 2. 🔍 Exact Search (точный поиск в БД)

**Кнопка:** Зеленая 🔍

**Как использовать:**
1. Введите **полное** название марки
2. Нажмите кнопку **🔍**

**Тип поиска:**
- Точное совпадение в БД (SQL: `WHERE grade = 'название'`)
- **БЕЗ AI**

**Примеры:**
```
Ввод: "K888" + 🔍 → Находит: только марку с именем "K888"
Ввод: "K"    + 🔍 → НЕ находит ничего (нет марки с точным именем "K")
Ввод: "D2"   + 🔍 → Находит: только марку "D2"
```

**Когда полезно:**
- Проверить наличие конкретной марки в БД
- Отфильтровать частичные совпадения
- Быстрая точная проверка

**Расход токенов:** ❌ НЕТ

---

### 3. 🤖 AI Search (поиск с Perplexity)

**Кнопка:** Фиолетовая 🤖

**Как использовать:**
1. Введите название марки (полное или частичное)
2. Нажмите кнопку **🤖**

**Тип поиска:**
1. Сначала ищет в БД (частичное совпадение)
2. Если НЕ нашел → вызывает **Perplexity API**
3. Perplexity ищет в интернете:
   - Официальные сайты производителей
   - MatWeb, steelnumber.com
   - PDF спецификации
   - Стандарты
4. Возвращает результат с химическим составом
5. Показывает кнопку "Добавить в БД"

**Требования к результату AI:**
- ✅ Обязательно: химический состав (минимум 1 элемент)
- ✅ Стандарт ИЛИ производитель с страной
- ✅ Ссылка на источник (сайт производителя > стандарт > PDF)

**Примеры:**
```
Ввод: "K888" + 🤖 → БД: не нашел → Perplexity:
  - Находит на сайте Bohler Edelstahl
  - Извлекает химический состав
  - Стандарт: "Bohler Edelstahl, Австрия"
  - Ссылка: https://www.bohler-edelstahl.com/en/products/k888-matrix/
  → Показывает результат с кнопкой "Добавить в БД"

Ввод: "Unknown123" + 🤖 → БД: не нашел → Perplexity:
  - Не нашел химический состав
  → Возвращает пустой результат (марка НЕ добавляется)
```

**Когда использовать:**
- Марки нет в БД
- Нужно найти неизвестную марку
- Хотите получить химический состав из интернета

**Расход токенов:** ✅ ДА (~$0.01 за поиск)

**Важно:**
- Используйте только для марок, которых точно нет в БД
- Сначала попробуйте обычный поиск или 🔍
- AI поиск медленнее (5-15 секунд)

---

## Сравнительная таблица

| Характеристика | Обычный поиск | 🔍 Exact Search | 🤖 AI Search |
|----------------|---------------|-----------------|--------------|
| **Кнопка** | Нет (автоматический) | Зеленая 🔍 | Фиолетовая 🤖 |
| **Тип поиска в БД** | LIKE '%...%' | WHERE grade='...' | LIKE '%...%' |
| **AI Fallback** | ❌ НЕТ | ❌ НЕТ | ✅ ДА (Perplexity) |
| **Скорость** | Мгновенно | Мгновенно | 5-15 секунд |
| **Расход токенов** | ❌ НЕТ | ❌ НЕТ | ✅ ДА (~$0.01) |
| **Когда использовать** | Поиск в БД | Точная проверка | Неизвестная марка |

---

## Визуальное отличие кнопок

```
┌────────────────────────────────────────────────┐
│  Grade: [K888_______________] 🔍 🤖 [Clear]   │
└────────────────────────────────────────────────┘

🔍 - Зеленая кнопка (Exact Search) - только БД
🤖 - Фиолетовая кнопка (AI Search) - БД + Perplexity
```

**Цвета кнопок:**
- 🔍 Exact Search: `#4caf50` (зеленый)
- 🤖 AI Search: `#9c27b0` (фиолетовый)
- Clear: `#6c757d` (серый)

---

## Типичные сценарии использования

### Сценарий 1: Поиск известной марки
```
1. Начните вводить "D2"
2. Автоматический поиск найдет марку в БД
3. Результат показан мгновенно
✅ Токены НЕ потрачены
```

### Сценарий 2: Проверка точного совпадения
```
1. Введите "K"
2. Обычный поиск покажет K888, K340, K110, и т.д.
3. Хотите только "K"? Нажмите 🔍
4. Результат: ничего не найдено (нет марки с именем "K")
✅ Токены НЕ потрачены
```

### Сценарий 3: Поиск неизвестной марки
```
1. Введите "K888"
2. Обычный поиск не нашел
3. Нажмите 🤖 AI Search
4. Ждите 5-15 секунд
5. Perplexity найдет марку в интернете
6. Показан результат с химическим составом
7. Нажмите "Добавить в БД"
⚠️ Токены потрачены: ~$0.01
```

### Сценарий 4: Быстрая проверка наличия
```
1. Введите полное название "Bohler K340"
2. Нажмите 🔍 (Exact Search)
3. Если нашел → марка есть в БД
4. Если не нашел → марки нет (можно попробовать 🤖)
✅ Токены НЕ потрачены
```

---

## Telegram бот vs Web интерфейс

| Функция | Telegram бот | Web интерфейс |
|---------|--------------|---------------|
| **Обычный поиск** | AI включен автоматически | Только БД |
| **AI Search** | Всегда активен | Только при нажатии 🤖 |
| **Кнопка добавления** | ✅ Есть | ✅ Есть |
| **Расход токенов** | На каждый запрос без результата | Только при нажатии 🤖 |
| **Использование** | Для конечных пользователей | Для администраторов/проверки |

---

## API параметры

### Обычный поиск (автоматический)
```http
GET /api/steels?grade=K888
```
- `ai` не передан → `use_ai=false`
- Только БД

### Exact Search (🔍)
```http
GET /api/steels?grade=K888&exact=true
```
- `exact=true` → точное совпадение в БД
- `ai` не передан → `use_ai=false`
- Только БД

### AI Search (🤖)
```http
GET /api/steels?grade=K888&ai=true
```
- `ai=true` → `use_ai=true`
- БД → Perplexity (если не нашел)

---

## Реализация в коде

### HTML (кнопки)
**Файл:** `templates/index.html:417-419`

```html
<input type="text" id="gradeFilter" placeholder="Enter steel grade">
<button class="btn-exact-search" onclick="searchExact()" title="Exact match in database">🔍</button>
<button class="btn-ai-search" onclick="searchWithAI()" title="AI Search (Perplexity)">🤖</button>
<button class="btn-clear-compact" onclick="clearFilters()" title="Clear all filters">Clear</button>
```

### JavaScript (функции)
**Файл:** `templates/index.html:596-809`

```javascript
// Обычный поиск (автоматический, при вводе)
function searchSteels(exactSearch = false, useAI = false) {
    const params = new URLSearchParams();
    if (grade) {
        params.append('grade', grade);
        if (exactSearch) params.append('exact', 'true');
        if (useAI) params.append('ai', 'true');
    }
    fetch(`/api/steels?${params.toString()}`)...
}

// 🔍 Exact Search
function searchExact() {
    searchSteels(true, false);  // exact=true, ai=false
}

// 🤖 AI Search
function searchWithAI() {
    searchSteels(false, true);  // exact=false, ai=true
}
```

### Backend (app.py)
**Файл:** `app.py:37-39`

```python
# AI Search enabled ONLY for explicit request
use_ai = request.args.get('ai', 'false').lower() == 'true'
```

---

## Рекомендации

### Для администраторов БД:
1. Используйте обычный поиск для проверки существующих марок
2. Используйте 🔍 для точной проверки наличия
3. Используйте 🤖 только для добавления новых марок

### Для экономии токенов:
1. Всегда сначала проверяйте обычным поиском
2. Если нашли частичное совпадение - используйте 🔍 для уточнения
3. Используйте 🤖 только если уверены, что марки нет в БД

### Для конечных пользователей:
- Рекомендуйте использовать Telegram бот (AI включен автоматически)
- Web интерфейс больше подходит для просмотра БД

---

## История изменений

### Версия 1 (до исправлений)
- AI включался автоматически на любой поиск
- Расход токенов на каждый частичный запрос

### Версия 2 (первое исправление)
- AI отключен для обычного поиска
- AI включался при 🔍 Exact Search (НЕПРАВИЛЬНО)

### Версия 3 (текущая)
- AI полностью отключен для обычного поиска
- AI отключен для 🔍 Exact Search
- AI включается ТОЛЬКО при нажатии 🤖 AI Search
- **Экономия токенов: 100% в обычном использовании**

---

## Заключение

✅ **Три четких типа поиска:**
1. Обычный (автоматический) - БД, частичное совпадение
2. 🔍 Exact Search - БД, точное совпадение
3. 🤖 AI Search - БД → Perplexity

✅ **Визуальное различие:**
- Зеленая 🔍 - только БД
- Фиолетовая 🤖 - с AI

✅ **Экономия токенов:**
- Токены тратятся ТОЛЬКО при нажатии 🤖
- 100% экономия в обычном использовании

✅ **Удобство:**
- Явное действие для AI поиска
- Нет конфликтов между типами поиска
- Понятно для пользователей
