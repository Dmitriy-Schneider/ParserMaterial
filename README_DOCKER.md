# 🐳 Docker инструкции для ParserSteel

## 📋 Требования

- Docker Desktop (Windows/Mac/Linux)
- Минимум 2GB RAM
- Порты 5001 свободен

## 🚀 Быстрый старт

### 1. Сборка образа

```bash
docker-compose build
```

### 2. Запуск контейнера

```bash
docker-compose up -d
```

### 3. Проверка работы

Откройте в браузере: http://localhost:5001

## 📝 Полезные команды

### Остановка контейнера
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs -f
```

### Пересборка образа (после изменения кода)
```bash
docker-compose up -d --build
```

### Просмотр запущенных контейнеров
```bash
docker ps
```

### Остановка и удаление контейнера (с сохранением данных)
```bash
docker-compose down
```

### Полная очистка (удаление контейнера и образа)
```bash
docker-compose down --rmi all
```

## 📦 Структура Docker

### Файлы
- `Dockerfile` - Описание образа приложения
- `docker-compose.yml` - Конфигурация для запуска
- `.dockerignore` - Исключаемые файлы из образа

### Volumes (сохраняемые данные)
- `./database` - База данных SQLite (сохраняется на хосте)
- `./logs` - Логи приложения

## 🔧 Настройка

### Изменить порт
Измените в `docker-compose.yml`:
```yaml
ports:
  - "5001:5000"  # Левый порт - хост, правый - контейнер
```

### Переменные окружения
Добавьте в `docker-compose.yml` в секцию `environment`:
```yaml
environment:
  - VARIABLE=value
```

## 🛠️ Запуск без Docker

Если нужно запустить локально без Docker:

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Запустить приложение
python app.py
```

## 📊 Статистика

После запуска контейнера:
- Приложение доступно на http://localhost:5001
- База данных сохраняется в папке `database/`
- API endpoint: http://localhost:5001/api/steels
- Статистика: http://localhost:5001/api/stats

## 🐛 Отладка

### Проблемы с запуском

1. **Порт занят**
```bash
# Найти процесс на порту 5001
netstat -ano | findstr :5001
# Завершить процесс или изменить порт в docker-compose.yml
```

2. **База данных не создается**
```bash
# Проверить права на папку database
# Убедиться что папка существует
mkdir database
```

3. **Контейнер не запускается**
```bash
# Проверить логи
docker-compose logs

# Попробовать пересборку
docker-compose up -d --build --force-recreate
```

## 🔄 Обновление

1. Получить последние изменения кода
2. Остановить контейнер: `docker-compose down`
3. Пересобрать образ: `docker-compose build`
4. Запустить: `docker-compose up -d`

## 📚 Дополнительная информация

- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)






