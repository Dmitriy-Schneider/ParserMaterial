# 🚀 Структура проекта ParserSteel для деплоя

## 📊 Анализ HTML кеш-файлов

### Текущее состояние:
- **reports/splav_cache/**: 3,327 HTML файлов, 52 MB
- **reports/metal_cache/**: 845 HTML файлов, 49 MB  
- **reports/zknives_pages/**: 277 MB (SHTML страницы)
- **Общий размер кеша**: ~378 MB

### Используются парсерами:
- `ru_splav_sync.py` → reports/splav_cache/
- `ru_metallicheckiy_sync.py` → reports/metal_cache/
- `zknives_page_sync.py` → reports/zknives_pages/

### ❗ ВЫВОД:
**HTML кеш НЕ нужен для работы марочника!**
- Кеш используется только для парсинга (one-time operation)
- Вся информация уже в базе данных (steel_database.db)
- База данных - единственный источник данных для Web и Telegram бота
- Кеш можно удалить или оставить в архиве для повторного парсинга

### ✅ РЕКОМЕНДАЦИЯ:
**Для деплоя на сервер** - НЕ переносить HTML кеш (экономия 378 MB)
**Для архива** - сохранить локально, если потребуется повторный парсинг

---

## 📁 Минимальная структура для деплоя

```
ParserSteel/
│
├── app.py                          # Flask API server (КРИТИЧНО)
├── telegram_bot.py                 # Telegram bot (КРИТИЧНО)
├── config.py                       # Конфигурация (КРИТИЧНО)
├── requirements.txt                # Python зависимости (КРИТИЧНО)
├── docker-compose.yml              # Docker конфигурация (КРИТИЧНО)
├── Dockerfile                      # Docker образ Flask API (КРИТИЧНО)
├── Dockerfile.telegram             # Docker образ Telegram (КРИТИЧНО)
├── .env                            # Environment variables (КРИТИЧНО, НЕ коммитить!)
│
├── database/
│   ├── steel_database.db           # Основная БД (КРИТИЧНО, 11 MB)
│   └── database_schema.py          # Schema management
│
├── static/                         # Web interface assets
│   ├── logo_white.svg              # Логотип (КРИТИЧНО)
│   └── logo_colored.svg
│
├── templates/
│   └── index.html                  # Web interface (КРИТИЧНО)
│
├── ai_search.py                    # AI Search через Perplexity/OpenAI
├── context_analyzer.py             # GPT-4o-mini для Telegram
│
└── reports/                        # CSV отчеты (опционально)
    ├── zknives_db_updates.csv      # 684 KB - ценные данные
    ├── splav_composition_compare.csv # 694 KB
    └── zknives_page_info.csv       # 980 KB

НЕ НУЖНО для деплоя:
├── reports/splav_cache/            # 52 MB HTML кеш (удалить)
├── reports/metal_cache/            # 49 MB HTML кеш (удалить)
├── reports/zknives_pages/          # 277 MB SHTML кеш (удалить)
├── archive/                        # Архивные файлы (не для деплоя)
├── docs/                           # Документация (не для деплоя)
└── tests/                          # Тесты (не для деплоя)
```

---

## 🔧 Критичные файлы (обязательны):

### 1. Docker конфигурация:
- `docker-compose.yml` - оркестрация контейнеров
- `Dockerfile` - образ Flask API
- `Dockerfile.telegram` - образ Telegram бота

### 2. Python приложения:
- `app.py` - Flask API server (10 KB)
- `telegram_bot.py` - Telegram bot (8.5 KB)
- `config.py` - конфигурация (1 KB)
- `ai_search.py` - AI Search (26 KB)
- `context_analyzer.py` - GPT анализ (5 KB)
- `database_schema.py` - DB schema (3 KB)

### 3. Зависимости:
- `requirements.txt` - Python packages

### 4. База данных:
- `database/steel_database.db` - 11 MB, 10,394 марок

### 5. Frontend:
- `templates/index.html` - Web UI
- `static/logo_white.svg` - логотип
- `static/logo_colored.svg`

### 6. Environment:
- `.env` - API ключи (TELEGRAM_TOKEN, PERPLEXITY_API_KEY, OPENAI_API_KEY)

---

## 📦 Размеры для деплоя:

| Компонент | Размер |
|-----------|--------|
| База данных | 11 MB |
| Python код | ~60 KB |
| Frontend | ~30 KB |
| CSV отчеты (опционально) | 2.3 MB |
| **ИТОГО** | **~13 MB** |

С HTML кешами: **~391 MB** ❌  
Без HTML кешей: **~13 MB** ✅ (экономия 97%)

---

## 🗑️ Очистка перед деплоем:

```bash
# Удалить HTML кеши (378 MB)
rm -rf reports/splav_cache/
rm -rf reports/metal_cache/
rm -rf reports/zknives_pages/

# Архивные файлы уже не нужны для деплоя
# (но не удаляем, оставляем в репозитории)
```

