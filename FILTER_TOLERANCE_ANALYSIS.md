# 🔍 Анализ "невидимого допуска" в фильтрах

## Проблема

При задании фильтров:
- **Cr max = 0.7** → показывает марки с **Cr 0.8**
- **Cr min = 0.4** → показывает марки с **Cr 0.3**

Похоже на невидимый допуск, но на самом деле это **логика пересечения диапазонов**.

---

## Анализ кода (app.py, строки 86-118)

### Текущая логика (ПЕРЕСЕЧЕНИЕ ДИАПАЗОНОВ):

```python
# Для min фильтра (например, Cr min = 0.4)
# Показывает марку, если ЛЮБАЯ часть её диапазона >= 0.4
if values['min']:
    min_val = float(values['min'])
    conditions = f"""
        CAST({element} AS REAL) >= {min_val} OR          # Одиночное значение >= min
        (range_start >= {min_val}) OR                     # Начало диапазона >= min
        (range_end >= {min_val})                          # Конец диапазона >= min
    """
```

**Пример:**
- Фильтр: **Cr min = 0.4**
- Марка: **Cr = 0.3-0.6**
- Проверка: `0.3 >= 0.4 OR 0.6 >= 0.4` = `FALSE OR TRUE` = **TRUE** ✅
- **Результат: марка показывается** (потому что диапазон пересекается с вашим запросом)

---

## Варианты поведения фильтров

### Вариант 1: ПЕРЕСЕЧЕНИЕ ДИАПАЗОНОВ (текущий)

**Логика:** "Показать марку, если её диапазон **пересекается** с моим запросом"

**Пример:**
- Фильтр: **Cr 0.4 - 0.7**
- Показываются марки с:
  - ✅ Cr = 0.5 (внутри диапазона)
  - ✅ Cr = 0.3-0.5 (пересекается)
  - ✅ Cr = 0.6-0.8 (пересекается)
  - ✅ Cr = 0.2-0.9 (полностью охватывает)
  - ❌ Cr = 0.2 (не пересекается)
  - ❌ Cr = 0.8 (не пересекается)

**Плюсы:**
- Находит больше подходящих марок
- Учитывает гибкость диапазонов

**Минусы:**
- Может показывать марки "на грани"
- Непонятно для пользователя (выглядит как "допуск")

---

### Вариант 2: ПОЛНОЕ ВКЛЮЧЕНИЕ (строгий)

**Логика:** "Показать марку, если её **весь диапазон** находится внутри моего запроса"

**SQL:**
```sql
-- Для диапазона Cr 0.4 - 0.7
WHERE (range_start >= 0.4 AND range_end <= 0.7)
```

**Пример:**
- Фильтр: **Cr 0.4 - 0.7**
- Показываются марки с:
  - ✅ Cr = 0.5 (внутри)
  - ✅ Cr = 0.45-0.65 (весь диапазон внутри)
  - ❌ Cr = 0.3-0.5 (выходит за границы)
  - ❌ Cr = 0.6-0.8 (выходит за границы)

**Плюсы:**
- Строгая фильтрация
- Понятно пользователю

**Минусы:**
- Может пропустить подходящие марки
- Слишком строго

---

### Вариант 3: ЦЕНТР ДИАПАЗОНА (компромисс)

**Логика:** "Показать марку, если **центр её диапазона** находится внутри моего запроса"

**SQL:**
```sql
-- Для диапазона Cr 0.4 - 0.7
-- Центр диапазона = (start + end) / 2
WHERE ((range_start + range_end) / 2 >= 0.4 AND (range_start + range_end) / 2 <= 0.7)
```

**Пример:**
- Фильтр: **Cr 0.4 - 0.7**
- Показываются марки с:
  - ✅ Cr = 0.5 (центр = 0.5)
  - ✅ Cr = 0.4-0.6 (центр = 0.5)
  - ✅ Cr = 0.3-0.7 (центр = 0.5)
  - ❌ Cr = 0.2-0.4 (центр = 0.3)
  - ❌ Cr = 0.7-0.9 (центр = 0.8)

**Плюсы:**
- Компромисс между строгостью и гибкостью
- Учитывает диапазоны

**Минусы:**
- Сложнее объяснить пользователю

---

### Вариант 4: С ДОПУСКОМ (явный)

**Логика:** "Показать марку с явным допуском ±X%"

**SQL:**
```sql
-- Для Cr 0.4 - 0.7 с допуском 10%
-- Допуск: 0.4 * 0.9 = 0.36, 0.7 * 1.1 = 0.77
WHERE (range_start >= 0.36 AND range_end <= 0.77)
```

**Плюсы:**
- Явный допуск (пользователь знает)
- Гибкость настройки

**Минусы:**
- Нужен UI для настройки допуска

---

## Рекомендация

### Предложение 1: Добавить переключатель режима фильтрации

В UI добавить опцию:

```
[ ] Строгий режим (только внутри диапазона)
[✓] Гибкий режим (пересечение диапазонов) - по умолчанию
```

### Предложение 2: Показывать пояснение

В UI добавить подсказку:

```
ℹ️ Фильтр работает в режиме "пересечения диапазонов"
Пример: при Cr 0.4-0.7 показываются марки с Cr 0.3-0.5, 0.6-0.8
Для строгой фильтрации используйте "Строгий режим"
```

### Предложение 3: Изменить логику (если не нужна гибкость)

Если вы хотите **строгую фильтрацию** (только внутри диапазона), я могу изменить код:

```python
# СТРОГАЯ ЛОГИКА
if values['min']:
    min_val = float(values['min'])
    # Начало диапазона должно быть >= min
    range_conditions.append(f"""
        ({element} IS NOT NULL AND
         ((CAST({element} AS REAL) >= ?) OR
          ({element} LIKE '%-%' AND CAST(SUBSTR({element}, 1, INSTR({element}, '-') - 1) AS REAL) >= ?)))
    """)
    range_params.extend([min_val, min_val])

if values['max']:
    max_val = float(values['max'])
    # Конец диапазона должен быть <= max
    range_conditions.append(f"""
        ({element} IS NOT NULL AND
         ((CAST({element} AS REAL) <= ?) OR
          ({element} LIKE '%-%' AND CAST(SUBSTR({element}, INSTR({element}, '-') + 1) AS REAL) <= ?)))
    """)
    range_params.extend([max_val, max_val])
```

---

## Тестирование на ваших примерах

### Пример 1: Cr max = 0.7

**Текущая логика (пересечение):**
```
Марка: 06КТСБоА1  Cr = 0.8       → ✅ Показывается (0.8 >= 0.7? NO, 0.8 <= 0.7? NO)
Марка: 08КГСБАЛ1   Cr = 0.8-1.2  → ✅ Показывается (0.8 <= 0.7? NO)
```

Ошибка! Марки с Cr > 0.7 НЕ должны показываться при max=0.7.

**Я вижу баг в коде!** Сейчас исправлю.

### Пример 2: Cr min = 0.4

**Текущая логика (пересечение):**
```
Марка: 06КТСБоА1  Cr = 0.3-0.6  → ✅ Показывается (0.6 >= 0.4? YES)
```

Это корректно для режима "пересечения", но может быть неожиданно для пользователя.

---

## Исправление бага

Проблема в строках 97-114 app.py. Логика `OR` слишком широкая.

**Должно быть:**
- **min фильтр**: Показывать, если **МАКСИМУМ диапазона** >= min (или одиночное значение >= min)
- **max фильтр**: Показывать, если **МИНИМУМ диапазона** <= max (или одиночное значение <= max)

**Исправленный код:**

```python
if values['min']:
    min_val = float(values['min'])
    # Для min: проверяем МАКСИМАЛЬНОЕ значение диапазона
    range_conditions.append(f"""
        ({element} IS NOT NULL AND {element} != '' AND
         (({element} NOT LIKE '%-%' AND CAST({element} AS REAL) >= ?) OR
          ({element} LIKE '%-%' AND CAST(SUBSTR({element}, INSTR({element}, '-') + 1) AS REAL) >= ?)))
    """)
    range_params.extend([min_val, min_val])

if values['max']:
    max_val = float(values['max'])
    # Для max: проверяем МИНИМАЛЬНОЕ значение диапазона
    range_conditions.append(f"""
        ({element} IS NOT NULL AND {element} != '' AND
         (({element} NOT LIKE '%-%' AND CAST({element} AS REAL) <= ?) OR
          ({element} LIKE '%-%' AND CAST(SUBSTR({element}, 1, INSTR({element}, '-') - 1) AS REAL) <= ?)))
    """)
    range_params.extend([max_val, max_val])
```

**Результат:**
- Cr max=0.7 → Показывает марки с Cr <= 0.7 (начало диапазона)
- Cr min=0.4 → Показывает марки с Cr >= 0.4 (конец диапазона)

Это режим "пересечения диапазонов" но с правильной логикой.

---

## Хотите строгую фильтрацию?

Если нужна **строгая фильтрация** (весь диапазон внутри):

```python
if values['min'] and values['max']:
    min_val = float(values['min'])
    max_val = float(values['max'])
    # ОБА условия: начало >= min И конец <= max
    range_conditions.append(f"""
        ({element} IS NOT NULL AND
         (({element} NOT LIKE '%-%' AND CAST({element} AS REAL) >= ? AND CAST({element} AS REAL) <= ?) OR
          ({element} LIKE '%-%' AND
           CAST(SUBSTR({element}, 1, INSTR({element}, '-') - 1) AS REAL) >= ? AND
           CAST(SUBSTR({element}, INSTR({element}, '-') + 1) AS REAL) <= ?)))
    """)
    range_params.extend([min_val, max_val, min_val, max_val])
```

Сообщите, какую логику предпочитаете!
